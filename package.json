{
  "name": "julien",
  "version": "0.0.1",
  "description": "",
  "main": "build/src/runner.js",
  "scripts": {
    "build": "node_modules/typescript/bin/tsc",
    "start": "node .",
    "develop": "nodemon src/runner.ts",
    "zip-lambda": "npm ci && npm run build && cp build/lambdaHandler.js build/index.js && cp -R node_modules build/ && cd build && zip -r ../extension-module.zip . && rm index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "AND Digital",
  "license": "MIT",
  "devDependencies": {
    "@types/express": "^4.17.13",
    "@types/node": "^16.9.4",
    "nodemon": "^2.0.12",
    "ts-node": "^10.2.1",
    "typescript": "^4.4.3"
  },
  "dependencies": {
    "@azure/functions": "^1.2.3",
    "@mollie/api-client": "^3.5.1",
    "express": "^4.17.1"
  }
}